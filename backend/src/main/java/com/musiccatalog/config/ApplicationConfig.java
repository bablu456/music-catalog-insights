package com.musiccatalog.config;

import org.springframework.context.annotation.Configuration;

/**
 * General application configuration for Spring Boot.
 * Acts as a placeholder for environment or framework-wide beans 
 * that do not fit into more specific configuration classes.
 */
@Configuration
public class ApplicationConfig {
    // Reserved for general @Bean definitions, application-wide properties mapping, etc.
}
